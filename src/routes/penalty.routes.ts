import { Router } from "express";

const router = Router();

// Define penalty-related routes here
// Example:
// router.get("/", (req, res) => res.send("Penalty routes placeholder"));

export default router;
