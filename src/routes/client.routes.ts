import { Router } from "express";

const router = Router();

// Define client-related routes here
// Example:
// router.get("/", (req, res) => res.send("Client routes placeholder"));

export default router;
