import { Router } from "express";

const router = Router();

// Define project-related routes here
// Example:
// router.get("/", (req, res) => res.send("Project routes placeholder"));

export default router;
