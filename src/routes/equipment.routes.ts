import { Router } from "express";

const router = Router();

// Define equipment-related routes here
// Example:
// router.get("/", (req, res) => res.send("Equipment routes placeholder"));

export default router;
